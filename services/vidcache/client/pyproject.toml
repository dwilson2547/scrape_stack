[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "dwilson-vidcache-client"
version = "1.3.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]
readme = "readme.md"

[tool.setuptools]
py-modules = ["vidcache_client"]
