storage:
  backend: local
  local:
    root: /data/filecache

request_auth:
  address: request-auth-server:9000
  enabled: true

ingest:
  temp_dir: /tmp/filecache
  chunk_size_mb: 1

server:
  host: 0.0.0.0
  port: 8030
